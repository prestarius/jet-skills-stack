@rules/working-agreement.md

# jet-skills (this repo)

- Read `HANDOFF.md` before changing anything; `BACKLOG.md` holds the prioritized work.
- Company-agnostic: no employer, client, or internal-project names in `skills/`, `agents/`,
  `hooks/`, `rules/`. Author identity is **Jet** only.
- Before committing, run `scripts/validate.sh`; it must print `OK`.
- Commit only when asked.
